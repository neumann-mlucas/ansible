- name: Create Syncthing shared directory
  ansible.builtin.file:
    path: "{{ syncthing_share_path }}"
    state: directory
    owner: "{{ username }}"
    group: "{{ username }}"
    mode: "0755"
  become: yes
