- name: Enable server systemd services
  ansible.builtin.systemd_service:
    name: "{{ item }}"
    enabled: true
    state: started
    masked: no
  loop:
    - jellyfin.service
    - pihole-FTL.service
